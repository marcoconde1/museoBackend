class Modelo {
    constructor(ruta_modelo, ruta_fondo, objeto_id) {
      this.ruta_modelo = ruta_modelo;
      this.ruta_fondo = ruta_fondo;
      this.objeto_id = objeto_id;
    }
  }
  
  module.exports = Modelo;
  